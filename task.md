# 생기부 작성 도우미 (School Life Record Helper) - 개발 진행 상황

## Phase 1: 프로젝트 초기화 및 랜딩 페이지 구축 (완료)
- [x] **프로젝트 초기화**
    - [x] Next.js 앱 초기화
    - [x] 전역 스타일 (Vanilla CSS) 및 폰트 설정
    - [x] 환경 변수 설정 (Gemini API)
- [x] **랜딩 페이지 구현**
    - [x] 헤더 및 네비게이션 (반응형)
    - [x] 개인정보처리방침 (로컬 스토리지 강조)
    - [x] 메인 히어로 섹션 ('by HooniKim' 스타일링)
    - [x] 메뉴 그리드 (4개 메인 아이템 + 설명)
    - [x] 크롬 확장 프로그램 및 유튜브 링크 섹션

## Phase 2: 랜딩 페이지 디자인 고도화 (완료)
- [x] **디자인 전면 개편**
    - [x] Tailwind 클래스 제거 및 Semantic CSS 적용
    - [x] 2x2 그리드 레이아웃 구현 (과세특, 동아리 / 행발, 가통)
    - [x] 카드 디자인 개선 (여백, 테두리, 그림자)
- [x] **UI 디테일 및 기능 개선**
    - [x] 'by HooniKim' 뱃지 위치/각도 정밀 조정
    - [x] 하단 섹션 레이아웃 변경 (나이스 자동입력 가로 배치, 유튜브 수직 배치)
    - [x] 유튜브 영상 16:9 반응형 비율 적용 및 임베드 구현
    - [x] 헤더 네비게이션 메뉴 추가 (우측 상단)

## Phase 3: 과세특(자유학기 세특) 페이지 기본 구현 (완료)
- [x] **기본 기능 구현**
    - [x] 페이지 라우팅 및 기본 레이아웃
    - [x] 활동 내용 입력 (동적 추가/삭제)
    - [x] 생성 옵션 (글자수, 전체 생성 버튼)
- [x] **AI 통합**
    - [x] Gemini API 설정 (gemini-2.0-flash-exp)
    - [x] 프롬프트 엔지니어링 (A/B/C 등급)
    - [x] 생성용 API 라우트
- [x] **기타 페이지 (플레이스홀더)**
    - [x] 동아리 세특 페이지 구현 (완료)
    - [x] 행발 작성, 가정통신문 작성 페이지 생성

## Phase 4: 과세특 페이지 UI/UX 전면 개편 (완료)
- [x] **레이아웃 및 디자인 재구성**
    - [x] Glassmorphism -> Section Card 스타일 변경
    - [x] 좌우 2열 배치 (학생 설정 / 활동 내용)
    - [x] 학생 목록 카드 디자인 개선 (삭제 버튼, 등급 버튼 등)
- [x] **학생 관리 기능 강화**
    - [x] 학생 수 드롭다운 및 '직접 입력' 기능 구현
    - [x] 엑셀 업로드 로직 개선 ('성명'/'이름' 컬럼 자동 인식 및 예외 처리)
    - [x] 텍스트박스 자동 높이 조절 (Auto-resize)

## Phase 5: 기능 디테일 및 로직 고도화 (최근 완료)
- [x] **엑셀 관련 기능 개선**
    - [x] 엑셀 다운로드 버튼 텍스트 추가 및 아이콘 정렬
    - [x] 빈 결과 다운로드 방지 로직 (알림창 표시)
- [x] **UI 사용성 개선**
    - [x] 삭제 버튼 위치 변경 (텍스트박스 상단 우측)
    - [x] 최소 1명 학생 유지 로직 (삭제 방지)
- [x] **생성 로직 고도화**
    - [x] 시스템 프롬프트 구체화 (교육 전문가 페르소나 적용)
    - [x] 짧은 글자수 제한 시 활동 내용 랜덤 선별 로직 추가 (2~3개)

## Phase 6: 프롬프트 엔지니어링 및 UX 고도화 (완료)
- [x] **프롬프트 및 AI 모델 최적화**
    - [x] AI 모델 변경 (gpt-4o -> gpt-4o-mini) 비용 절감 및 속도 개선
    - [x] 학교급(초/중/고) 및 과목명 입력 기능 추가 및 프롬프트 반영
    - [x] 프롬프트 정교화 (학생 호칭 제거, C등급 피드백 개선, 요약/서두 언급 금지)
    - [x] 글자수 제한 로직 강화 (등급별 차등 적용, 600byte 엄격 준수)
    - [x] 활동 내용 랜덤 셔플 및 선별 로직 (글자수에 따른 개수 조절)
- [x] **UI/UX 편의성 개선**
    - [x] 활동 입력 엔터키 연동 및 오토 포커스 구현
    - [x] 결과창 스크롤바 적용 (최대 높이 500px 제한)
    - [x] 개별 생성 버튼 위치 이동 및 '내용 지우기' 버튼 추가
    - [x] 학생 삭제 아이콘 변경 (UserX) 및 툴팁 추가
    - [x] 전체 생성 완료 시 재생성 확인 팝업 및 생성 상태(Loading) 오류 수정
    - [x] 파비콘 아이콘 변경 (H 로고, 투명 배경)

## Phase 7: 동아리 세특 및 행발 페이지 구현 (완료)
- [x] **동아리 세특 페이지 구축**
    - [x] 과세특 페이지 UI/UX 계승 (통일성 유지)
    - [x] 학생 등급(A/B/C) 선택 기능 제거
    - [x] 동아리명 입력 필드 적용
    - [x] 동아리 전용 프롬프트 적용 및 관점(Perspective) 다양화 로직 구현
- [x] **행발 작성 페이지 구축**
    - [x] 활동 내용 입력 섹션 제거 및 생성 옵션 재배치
    - [x] 학생별 '행동 관찰 결과' 입력 필드 추가
    - [x] 행발 전용 프롬프트(긍정적 재구성 등) 적용
    - [x] 엑셀 업로드 개선 ('행동', '관찰' 컬럼 자동 인식 및 매핑)
- [x] **가정통신문 작성 페이지 구축**
    - [x] 여름방학/겨울방학 선택 옵션 구현
    - [x] 공통 키워드 입력 필드 추가 (기본값: 학업, 건강 등)
    - [x] 학생별 개별 입력 필드 제거 (일괄 생성 방식)
    - [x] 방학별 맞춤형 프롬프트 적용
- [x] **세부 설정 및 로직 검증**
    - [x] 과세특 학생 기본 등급 변경 (B -> A)
    - [x] 동아리 세특 학교급 기본값 변경 (고등학교 -> 중학교)
    - [x] 글자수 제한에 따른 활동 선별 로직 검증 완료
    - [x] 전 페이지 글자수 제한 로직 강화 (공백 포함 엄격 준수)

## Phase 8: 최종 검증 및 배포 준비 (완료)
- [x] **최종 테스트**
    - [x] 모바일/태블릿 반응형 최종 점검 (CSS 미디어 쿼리 확인 완료)
    - [x] 엑셀 대량 데이터 처리 테스트 (파싱 로직 검증 완료)
    - [x] AI 생성 품질 최종 확인 (프롬프트 최적화 완료)

# 프로젝트 완료 (Web App)
모든 웹 애플리케이션 개발 단계가 완료되었습니다.
- 과세특, 동아리 세특, 행발, 가정통신문 작성 기능 구현 완료.
- UI/UX 통일성 확보 및 반응형 디자인 적용.
- AI 프롬프트 최적화 및 글자수 제한 로직 강화.

## Phase 9: 크롬 확장 프로그램 개발 (완료)
- [x] **확장 프로그램 기본 설정**
    - [x] Manifest V3 설정 (`manifest.json`)
    - [x] 아이콘 및 기본 리소스 구성 (아이콘 누락 및 리사이징 수정 완료)
    - [x] **[NEW] 명칭 및 용어 변경 (ACE -> 나이스 자동입력 도우미, 교과평어 -> 과세특, 행동특성 -> 행발)**
- [x] **UI 구현 (Popup)**
    - [x] 디자인 시안(이미지) 기반 UI 구현 (HTML/CSS)
    - [x] **[NEW] UI 디자인 전면 개편 (Web App 스타일 통일, Outfit 폰트, 카드 레이아웃)**
    - [x] 상태 표시창 (데이터 개수 확인)
    - [x] 데이터 가져오기 / 자동 입력 버튼 구현
- [x] **기능 구현: 데이터 가져오기 (From Web App)**
    - [x] Content Script: 웹 앱 DOM 파싱 및 데이터 추출
    - [x] Chrome Storage 저장 로직
- [x] **기능 구현: 자동 입력 (To NEIS)**
    - [x] Content Script: NEIS 입력 필드 식별 및 값 주입
    - [x] 자동 입력 순회 로직 (매크로 성격)
    - [x] **[NEW] 가상 스크롤 및 그리드 대응 (Scroll, ArrowDown, Enter 전략 적용)**

## Phase 10: NEIS 자동 입력 고도화 및 디버깅 (완료 ✅)
- [x] **이름 기반 매칭 시스템 구현**
    - [x] 행 순서 대신 학생 이름으로 매칭하는 `processAutoFillByName` 함수 구현
    - [x] `findStudentRow` 함수: 그리드에서 학생 이름 검색 및 스크롤 지원
    - [x] 가상 스크롤 대응: DOM에 없는 학생은 스크롤하여 검색
- [x] **NEIS Floating Textarea 대응**
    - [x] NEIS가 row 내부가 아닌 floating textarea 사용함을 발견
    - [x] `aria-label` 속성에서 행 번호 추출 (`N행 마지막 열...` 형식)
    - [x] `data-rowindex`와 `aria-label` 행 번호 매핑 로직 구현
    - [x] `findActiveTextareaInDocument` 함수: 전체 document에서 활성 textarea 검색
- [x] **Textarea 활성화 로직 강화**
    - [x] 다중 활성화 전략: Direct click, Double-click, Focus+Enter, Focus+F2, Control wrapper click
    - [x] 스크롤 후 row 재검색 로직 (가상 스크롤로 DOM 변경 대응)
    - [x] ~~ESC 키 + blur로 강제 닫기~~ → **Tab 키로 변경 (데이터 저장 보장)**
    - [x] 좌표 기반 MouseEvent 시뮬레이션 (8-cell 행 대응)
- [x] **셀 탐색 로직 개선 (8 cells 구조 대응)**
    - [x] Strategy 1: row 내 기존 editable textarea 확인
    - [x] Strategy 2: `.cl-control.cl-textarea` (non-readonly) 검색
    - [x] Strategy 3: 모든 셀에서 textarea control 검색
    - [x] Strategy 4: `aria-label`에서 '세부능력'/'특기사항' 키워드로 셀 탐색
    - [x] Final fallback: 8 cells → cell 7, 7 cells → cell 5 사용
- [x] **장기결석 행 이후 8-cell 행 특별 처리**
    - [x] 8-cell 감지 시 **Tab 네비게이션 전략** 적용 (NEIS 자동 스크롤 활용)
    - [x] 현재 textarea에서 Tab 키로 다음 행 이동
    - [x] 정확한 aria-row 매칭 필수 (±2 tolerance 제거)
    - [x] scrollIntoView fallback 적용
- [x] **Readonly 셀 대응 (분할된 행 처리)**
    - [x] 1-cell readonly 행 감지 시 Tab 네비게이션으로 다음 textarea 이동
    - [x] 마지막 학생(허지훈 등) 정상 입력 완료
- [x] **테스트 및 검증 완료**
    - [x] 26/26명 (100%) 입력 성공 확인
    - [x] 장기결석 행 이후 학생 입력 정상 동작 확인

### 최종 상태 (2025-12-06) ✅
- **26/26명 (100%) 입력 성공**
- 모든 문제 해결됨:
  - 장기결석 행 이후 8-cell 행: Tab 네비게이션으로 NEIS 자동 스크롤 활용
  - Readonly 분할 행: Tab 키로 다음 입력 가능 셀 이동
  - 데이터 손실 방지: ESC → Tab 변경으로 저장 보장

# 프로젝트 완료 (Chrome Extension) 🎉
NEIS 자동 입력 크롬 확장 프로그램 개발이 완료되었습니다.
- 웹앱에서 생성된 데이터를 NEIS에 자동 입력하는 기능 구현 완료.
- 가상 스크롤, 8-cell 행, readonly 셀 등 NEIS 특수 구조 완벽 대응.
- Tab 네비게이션 전략으로 안정적인 자동 입력 구현.

## Phase 11: 글자수 제한 엄격 준수 기능 강화 (완료)
- [x] **프롬프트 강화 (전체 페이지 적용)**
    - [x] 글자수 제한 지시문 전면 개편 (최우선 준수 사항으로 강조)
    - [x] "절대 규칙", "초과시 무효화" 등 강력한 표현 사용
    - [x] 작성 전/후 글자수 확인 지시 추가
- [x] **활동 선별 로직 강화 (과세특, 동아리 세특)**
    - [x] 80자 미만: 활동 1개만 선택
    - [x] 150자 이하: 최대 2개 선택
    - [x] 250자 이하: 최대 3개 선택
    - [x] 350자 이하: 최대 4개 선택
    - [x] 350자 초과: 모든 활동 사용
- [x] **후처리 함수 추가 (전체 페이지 적용)**
    - [x] `truncateToCharLimit()` 함수 구현
    - [x] AI 응답이 글자수 초과 시 자동으로 자르기
    - [x] 마지막 완전한 문장(마침표)에서 자르기 (70% 이후)
    - [x] 불완전한 경우 마침표 자동 추가 (~함. ~임. 등 종결)
    - [x] 콘솔 로그로 자르기 작업 기록

### 적용 페이지
- 과세특 (`/gwasetuk`) ✅
- 동아리 세특 (`/club`) ✅
- 행발 작성 (`/behavior`) ✅
- 가정통신문 (`/letter`) ✅

## Phase 12: 행발 페이지 엑셀 업로드 기능 개선 (완료)
- [x] **엑셀 파싱 로직 개선**
    - [x] 헤더 행 디버깅 로그 추가 (열 인덱스 및 열 이름 출력)
    - [x] 행동 관찰 결과 열 인식 키워드 확장
        - 기존: 행동, 관찰, 특성
        - 추가: 관찰결과, 행동관찰, 행발, 결과, 종합의견
    - [x] 파싱 결과 디버깅 로그 추가 (nameColIndex, observationColIndex, headerRowIndex)
    - [x] 학생별 관찰결과 로그 출력

### 인식 가능한 열 이름 키워드
| 키워드 | 예시 열 이름 |
|--------|-------------|
| 관찰결과 | 관찰 결과, 행동 관찰 결과 |
| 행동관찰 | 행동 관찰 내용 |
| 행발 | 행발, 행발 내용 |
| 행동 | 행동 특성 |
| 관찰 | 관찰 기록 |
| 결과 | 관찰결과 |
| 특성 | 행동특성 |
| 종합의견 | 종합의견 |

## Phase 13: 과세특 페이지 학생별 개별 활동 기능 추가 (완료)
- [x] **Student 상태 구조 확장**
    - [x] `individualActivity` 필드 추가 (초기값 및 updateStudentList 함수 모두 적용)
- [x] **엑셀 업로드 로직 수정**
    - [x] '활동 내용' 열 자동 인식 추가
    - [x] 인식 키워드: '활동', '내용', '관찰내용', '관찰기록'
    - [x] 학생 데이터에 individualActivity 필드 포함하여 저장
- [x] **학생 카드 UI 수정**
    - [x] 등급 버튼(A/B/C)과 액션 버튼 사이에 textarea 추가
    - [x] placeholder: "학생별 개별적으로 활동한 내용을 입력해주세요."
- [x] **생성 로직 수정**
    - [x] `calculateRelevanceScore()` 함수 추가 (키워드 매칭으로 유사도 계산)
    - [x] 공통 활동과 학생별 개별 활동을 조합하여 프롬프트 생성
    - [x] 글자수 제한 시 학생별 활동과 관련성 높은 공통 활동 우선 선택

## Phase 14: AI 텍스트 생성 후처리 로직 고도화 (완료)
- [x] **공통 유틸리티 추가**
    - [x] `textProcessor.js` 파일 추가 (AI 텍스트 후처리 전용 모듈)
    - [x] 동적 버퍼 비율 적용 (글자수에 따라 자동 조절)
    - [x] 글자수별 맞춤 프롬프트 생성
    - [x] 완전한 문장에서 자르기 (트리밍 로직 개선)
- [x] **과세특 엑셀 업로드 키워드 확장**
    - [x] 신규 인식 키워드 추가: '세부능력', '특기사항', '세특', '개별활동'
    - [x] 세부능력/특기사항/세특 열 자동 인식 강화

### 적용 페이지
- 과세특 (`/gwasetuk`) ✅
- 동아리 세특 (`/club`) ✅
- 행발 작성 (`/behavior`) ✅
- 가정통신문 (`/letter`) ✅

## Phase 15: 동아리 세특 페이지 학생별 개별 활동 기능 추가 (완료)
- [x] **Student 상태 구조 확장**
    - [x] `individualActivity` 필드 추가 (초기값 및 `updateStudentList` 함수 모두 적용)
- [x] **엑셀 업로드 로직 수정**
    - [x] '활동 내용' 열 자동 인식 추가
    - [x] 인식 키워드: '활동', '내용', '관찰내용', '관찰기록', '세부능력', '특기사항', '세특', '개별활동'
    - [x] 학생 데이터에 `individualActivity` 필드 포함하여 저장
- [x] **학생 카드 UI 수정**
    - [x] 개별 활동 textarea 추가 (placeholder: "학생별 개별적으로 활동한 내용을 입력해주세요.")
- [x] **생성 로직 수정**
    - [x] `calculateRelevanceScore()` 함수 추가 (키워드 매칭으로 유사도 계산)
    - [x] 공통 활동과 학생별 개별 활동을 조합하여 프롬프트 생성
    - [x] 글자수 제한 시 학생별 활동과 관련성 높은 공통 활동 우선 선택

## Phase 16: 과세특 페이지 추가 지침 사항 기능 추가 (완료)
- [x] **State 추가**
    - [x] `additionalInstructions` state 추가
- [x] **프롬프트 연동**
    - [x] AI 프롬프트에 최우선 지침 섹션 추가 (다른 모든 규칙보다 우선 적용)
- [x] **UI 구현**
    - [x] 활동 입력 섹션 하단에 빨간색 테마 textarea 추가
    - [x] placeholder: "예: 축구는 단체 경기가 아닌 개인별 수행 내용을 기준으로 작성해 주세요."
    - [x] 안내 문구: "위 지침은 AI가 최우선으로 엄격히 준수합니다."
    - [x] 스타일: borderColor '#fecaca', backgroundColor '#fef2f2' (빨간색 테마)

## Phase 17: 랜딩 페이지 사용 안내 박스 추가 (완료)
- [x] **프리미엄 스타일 디자인 구현**
    - [x] 배경: 연한 파란색에서 연한 보라색 그라데이션 (linear-gradient)
    - [x] 상단 테두리: 파란색→보라색 그라데이션 라인 (3px)
    - [x] 왼쪽 정보 아이콘 (그라데이션 배경)
    - [x] 제목: 그라데이션 텍스트 + 문의 이메일 안내
- [x] **번호 목록 스타일**
    - [x] 메인 항목: 원형 그라데이션 배지 안에 숫자 (1, 2, 3)
    - [x] 하위 목록: 보라색 점 불릿
- [x] **3가지 강조 표시 색상**
    - [x] `.emphasis`: 파란색 계열 - 중요 기능명, 키워드
    - [x] `.emphasis-purple`: 보라색 계열 - 서비스/기능 이름
    - [x] `.emphasis-orange`: 주황색 계열 - 주의사항, 중요 동작
- [x] **반응형 디자인**
    - [x] 모바일: 아이콘과 내용 세로 배치
- [x] **내용 구성**
    - [x] 1. 하단 사용 방법 영상 시청 안내
    - [x] 2. 영상 외 추가 내용 (4개 하위 항목)
        - 엑셀파일 업로드로 자동 학생/내용 추가
        - 학생별 개별 활동 입력 기능
        - 개별/공통 활동 조합 생성
        - 추가 지침 사항 기능
    - [x] 3. 나이스 확장프로그램 사용 시 같은 브라우저 안내

## Phase 18: 동아리 세특 페이지 추가 지침 사항 기능 추가 (완료)
- [x] **State 추가**
    - [x] `additionalInstructions` state 추가
- [x] **프롬프트 연동**
    - [x] AI 프롬프트에 최우선 지침 섹션 추가 (다른 모든 규칙보다 우선 적용)
    - [x] 절대 금지사항 섹션 뒤, 대상 학교급 앞에 추가 지침 삽입
- [x] **UI 구현**
    - [x] 활동 추가 버튼 하단에 빨간색 테마 textarea 추가
    - [x] placeholder: "예: 토론 활동은 주제와 본인의 입장을 중심으로 작성해 주세요."
    - [x] 안내 문구: "위 지침은 AI가 최우선으로 엄격히 준수합니다."
    - [x] 스타일: borderColor '#fecaca', backgroundColor '#fef2f2' (빨간색 테마)


## Phase 19: Gemini 모델 마이그레이션 (완료)
- [x] **Gemini 2.5 Flash 업그레이드**
    - [x] Gemini 2.0 Flash 지원 중단 공지에 따른 모델 변경 (`gemini-2.0-flash-exp` -> `gemini-2.5-flash`)
    - [x] API 라우트(`app/api/generate/route.js`) 엔드포인트 수정
## Phase 20: 프롬프트 최적화 및 추가 지침 강화 (완료)
- [x] **마무리 요약 문장 금지 강화**
    - [x] 핵심 원칙 추가: "오직 활동 내용만 서술하고, 마무리/요약/정리 문장은 절대 작성하지 않음"
    - [x] 금지 표현 목록 확장: '이러한', '이를 통해', '이와 같이', '이런', '앞으로', '향후', '결과적으로', '종합적으로'
    - [x] "마지막 문장도 반드시 구체적인 활동 내용이나 학습 과정에 대한 서술이어야 함" 지시 추가
- [x] **추가 지침 우선순위 강화**
    - [x] 추가 지침 위치를 프롬프트 **가장 마지막**으로 이동 (AI가 최우선 적용하도록)
    - [x] 강조 형태 개선: `【🚨 특별 지시 - 반드시 적용 🚨】`

## Phase 21: 랜덤 셔플 및 활동 순서 로직 개선 (완료)
- [x] **'랜덤' 키워드 감지 시 활동 셔플**
    - [x] 추가 지침에 '랜덤' 또는 '무작위' 키워드 포함 시 활동 목록 셔플
    - [x] AI가 위에서부터 선택해도 결과적으로 랜덤 효과 발생
- [x] **기본 순서 유지**
    - [x] 개별 활동이 없을 경우 기본적으로 입력 순서를 유지하도록 변경

## Phase 22: 결과 복사 버튼 기능 추가 (완료)
- [x] **복사 버튼 UI 구현**
    - [x] 과세특, 동아리, 행발 페이지 생성 결과창 하단에 "복사" 버튼 추가
    - [x] 클릭 시 "복사됨!" 피드백 표시 (1.5초 후 복구)
- [x] **클립보드 API Fallback 구현**
    - [x] `navigator.clipboard` 미지원 환경(HTTP 등)에서도 작동하도록 fallback 로직 적용
## Phase 23: Gemini 모델 속도 최적화 (완료)
- [x] **Gemini 2.5 Flash Lite 적용**
    - [x] 생성 속도 개선을 위해 모델 변경 (`gemini-2.5-flash` -> `gemini-2.5-flash-lite`)
    - [x] API 라우트(`app/api/generate/route.js`) 엔드포인트 수정

## Phase 24: 생성 글자수 정상화 및 설정 파일 최적화 (완료)
- [x] **생성 글자수 정상화**
    - [x] AI 생성 결과의 글자수가 설정된 제한에 맞게 출력되도록 로직 정상화
- [x] **OpenAI API 기반 설정 파일 복사**
    - [x] OpenAI API를 사용하는 동일한 웹앱의 설정 파일을 복사하여 적용

## Phase 25: 다중 로컬 LLM 지원 및 UI 추가 (완료)
- [x] **다중 모델 선택기 구현**
    - [x] `utils/streamFetch.js`에 `AVAILABLE_MODELS` 설정 추가 (6종 모델 지원)
    - [x] 과세특, 동아리 세특, 행발, 가통 4개 페이지 생성 옵션에 'AI 모델' 드롭다운 추가
    - [x] 기본 모델을 `qwen3:8b` (추천)로 설정
- [x] **API 연동 최적화**
    - [x] `fetchStream`에서 클라이언트가 선택한 모델 ID를 Ollama API로 전달

## Phase 26: 로컬 LLM 프롬프트 및 후처리 최적화 (완료)
- [x] **생성 프롬프트 구조 전면 개편**
    - [x] 부정 지시어("~하지 마세요")를 긍정 지시어("~하세요", "~대신 ~를 서술")로 변경
    - [x] `<입력 정보>`, `<작성 규칙>`, `<출력 형식>`, `<좋은 예시>` 형태의 XML 태그 기반 구조화
    - [x] 로컬 LLM(8B~12B)이 이해하기 쉽도록 1개의 명확한 '좋은 예시' 추가
- [x] **추가 지침 (Sandwich 기법) 적용**
    - [x] `streamFetch.js`에서 추가 지침을 시스템 메시지, user 프롬프트 맨 앞, 맨 뒤 총 3곳에 삽입하여 무시 방지
- [x] **짧은 문자 생성 품질 개선**
    - [x] 200자 이하 생성 시 "각 문장은 20자 이내" 등 파편화 지시문을 제거하고, "3~4개의 온전한 문장" 지시 및 예시문 추가
- [x] **완전한 문장 재시도(Retry) 로직 추가**
    - [x] AI 응답이 완전한 한국어 종결어미(`함.`, `음.` 등)로 끝나지 않을 경우, 이전 응답을 보여주며 최대 2회 재시도 (API 레벨)
    - [x] `textProcessor.js`의 `truncateToCompleteSentence` 코드 레벨 후처리와 결합된 2중 안전망 확보

## Phase 27: 로컬 LLM 모델 목록 재정비 (2026-04-03) (완료)
- [x] **공통 모델 목록 재정비**
    - [x] `utils/streamFetch.js`에서 `llama3.1:8b`, `gemma3:12b-it-q4_K_M` 제거
    - [x] `gemma4:e4b`, `gemma4:e2b` 추가 후 전체 목록을 6개 기준 모델로 정리
    - [x] 기본 모델을 `gemma4:e4b`로 변경하고, 기본 모델 대비 속도/품질 기준 설명으로 재작성
    - [x] 기본 모델을 최상단에 두고, 나머지 모델은 속도 기준으로 재배치
- [x] **모델 선택 UI 라벨 통일**
    - [x] `utils/streamFetch.js`에 공통 드롭다운 라벨 함수 추가
    - [x] 과세특, 동아리 세특, 행발, 가정통신문 페이지의 모델 선택 드롭다운을 `모델명 + 설명` 형식으로 통일
- [x] **문서 및 예시 동기화**
    - [x] `ollama.md`의 모델 예시, keepalive 명령, curl 점검 예시를 현재 기본 모델 기준으로 갱신
    - [x] `local-llm-api-guide.md`를 추가해 현재 코드와 동일한 모델 목록/기본값/라벨 규칙을 정리
    - [x] `app/api/generate/route.js`의 하드코딩 기본 모델도 공통 기본값과 동기화
## Phase 28: 생성 길이 490자 및 max_tokens 보정 (2026-04-07) (완료)
- [x] **1500byte preset 490자 기준으로 조정**
    - [x] 과세특, 행발, 동아리, 가정통신문 4개 페이지 모두 `1500byte` 선택 시 목표 글자수를 `490`자로 통일
    - [x] 수동 입력 기본 fallback 값도 `490`자로 동기화
    - [x] 드롭다운 라벨을 `1500byte (한글 약 490자)` 기준으로 정리
- [x] **공통 API 출력 길이 상한 보강**
    - [x] `utils/streamFetch.js`에서 `targetChars`를 받아 요청마다 `max_tokens`를 동적으로 전달
    - [x] `utils/textProcessor.js`에 목표 글자수 기반 `max_tokens` 추정 함수 추가
    - [x] large preset 프롬프트 목표 비율을 상향 조정해 너무 짧은 출력이 나오지 않도록 보완
- [x] **디버깅 가시성 추가**
    - [x] Ollama 호환 응답의 `finish_reason`이 `length`일 때 콘솔 경고를 남기도록 추가

## Phase 29: 로컬 LLM 모델 목록 3종 압축 (2026-04-24) (완료)
- [x] **공통 모델 목록 3종 정리**
    - [x] `utils/streamFetch.js`의 `AVAILABLE_MODELS`를 `gemma4:e4b`, `gemma4:e2b`, `gemma4:26b` 3개로 압축
    - [x] 기본 모델은 기존과 동일하게 `gemma4:e4b`로 유지
    - [x] 과세특, 동아리 세특, 행발, 가정통신문 페이지가 공통 모델 목록을 통해 동일한 3개 선택지만 표시하도록 확인
- [x] **문서 예시 동기화**
    - [x] `ollama.md`와 `local-llm-api-guide.md`의 현재 모델 목록 예시를 동일한 3종 기준으로 수정

## Phase 30: OpenAI API key 기반 생성 경로 추가 (2026-04-24) (완료)
- [x] **네 화면 공통 OpenAI key 입력 UI 추가**
    - [x] 과세특, 동아리 세특, 행발, 가정통신문의 `생성 옵션`에 OpenAI API key 입력/적용/해제 영역 추가
    - [x] 한 화면에서 key를 적용하면 `localStorage`를 통해 네 화면 모두에서 같은 key를 사용하도록 공통 훅 구성
- [x] **OpenAI 생성 경로 추가**
    - [x] `/api/openai-generate` route를 추가해 브라우저에 key를 하드코딩하지 않고 요청 시 서버 경유로 OpenAI API 호출
    - [x] key가 적용된 상태에서 `AI 생성`을 누르면 OpenAI API로 생성하고, key가 없으면 기존 로컬 LLM 생성 유지
    - [x] 생성 중 버튼과 학생별 생성 상태에 `OpenAI API key를 사용하여 생성 중...` 표시 추가

## Phase 31: OpenAI 기본 모델 GPT-5.4 mini 변경 (2026-04-24) (완료)
- [x] **OpenAI 기본 모델 갱신**
    - [x] OpenAI API 생성 기본 모델을 `gpt-5.4-mini`로 변경
    - [x] 화면 표시 라벨을 `GPT-5.4 mini`로 동기화
    - [x] 과세특과 동아리 세특의 OpenAI 생성 프롬프트 분기용 모델 문자열도 `openai:gpt-5.4-mini`로 변경

## Phase 32: OpenAI 모델 선택 및 생성 옵션 정렬 보정 (2026-04-24) (완료)
- [x] **OpenAI 모델 선택 추가**
    - [x] OpenAI key 영역에서 `GPT-5.4 mini`와 `GPT-5 mini` 중 선택할 수 있도록 공통 선택 UI 추가
    - [x] 선택한 OpenAI 모델을 `localStorage`로 공유하여 네 화면에서 동일하게 적용
    - [x] `/api/openai-generate`가 선택한 모델 ID를 검증한 뒤 해당 모델로 호출하도록 수정
- [x] **과세특/동아리 세특 생성 옵션 정렬 보정**
    - [x] `grid-2-cols items-end`로 인해 글자수 제한과 버튼이 아래로 밀리던 문제를 상단 정렬로 조정

## Phase 33: 생성 옵션 버튼 위치 및 로컬 모델 라벨 정리 (2026-04-24) (완료)
- [x] **과세특/동아리 세특 생성 옵션 배치 보정**
    - [x] 글자수 제한과 `AI 생성`/`엑셀` 버튼을 같은 왼쪽 컬럼으로 묶어 버튼 줄이 OpenAI 설정 높이에 밀려 내려가지 않도록 수정
    - [x] 오른쪽 컬럼은 `로컬 AI 모델`, `OpenAI 모델`, `OpenAI API key` 순서로 유지
- [x] **로컬 LLM 라벨 명확화**
    - [x] 과세특, 동아리 세특, 행발, 가정통신문 네 화면의 기존 `AI 모델` 라벨을 `로컬 AI 모델`로 변경

## Phase 34: OpenAI 기본 모델 GPT-5 mini 변경 (2026-04-24) (완료)
- [x] **기본 OpenAI 모델 비용 절감 방향으로 변경**
    - [x] OpenAI 모델 목록에서 `GPT-5 mini`를 기본 선택값으로 변경
    - [x] `GPT-5.4 mini`는 선택 가능한 보조 모델로 유지
    - [x] 서버 fallback 로직이 기본 모델과 같은 모델을 반복 호출하지 않도록 대체 모델 선택 방식으로 보정

## Phase 35: 내부 생성 검증 하네스 추가 (2026-04-24) (완료)
- [x] **공통 검증기 구현**
    - [x] `utils/generationHarness.js`를 추가해 글자수, 줄바꿈, 금지 주어, 금지 용어, 과거형, 요약 표현, 메타 문구, 문장 종결을 내부 검증
    - [x] 사용자에게 별도 UI를 노출하지 않고 최대 2회까지 조용히 재생성하는 `generateWithSilentValidation` 흐름 추가
    - [x] 자동 재생성 후에도 미흡한 결과는 코드 후처리로 한 번 더 정리
- [x] **네 생성 화면에 공통 적용**
    - [x] 과세특, 동아리 세특, 행발, 가정통신문의 로컬 LLM과 OpenAI 생성 결과 모두 같은 검증 하네스를 통과하도록 연결
    - [x] 과세특은 과목명, 동아리 세특은 동아리명, 공통으로 학생 이름을 출력 금지 용어로 검증
    - [x] 가정통신문 로컬 LLM 경로는 경어체와 가정통신문 형식에 맞는 전용 시스템 메시지를 사용하도록 분리
- [x] **검증 테스트 추가**
    - [x] `tests/generationHarness.test.mjs`를 추가해 규칙 위반 감지와 조용한 재시도 동작을 검증
    - [x] `npm test` 스크립트를 추가해 내부 검증 테스트를 바로 실행 가능하게 구성

## Phase 36: 글자수 제한 정규화 및 테스트 보강 (2026-04-25) (완료)
- [x] **글자수 제한 적용 경로 재검사**
    - [x] 네 화면의 프리셋 값이 `1500byte=490자`, `1000byte=330자`, `600byte=200자`로 전달되는지 확인
    - [x] 로컬 LLM과 OpenAI 생성 모두 `max_tokens`, 프롬프트 지침, 내부 검증, 최종 후처리를 통해 같은 제한을 적용하는지 확인
- [x] **직접 입력값 정규화 보강**
    - [x] `normalizeTargetChars()`를 추가해 직접 입력값을 생성 전 `1~490자` 범위로 정규화
    - [x] 과세특, 동아리 세특, 행발, 가정통신문의 중복 글자수 계산 로직을 공통 함수로 통일
- [x] **글자수 테스트 추가**
    - [x] `tests/textProcessor.test.mjs`를 추가해 프리셋/직접입력 정규화와 최종 절단 결과가 제한을 넘지 않는지 검증

## Phase 37: 추가 지침 사항 적용 경로 검증 및 OpenAI 보강 (2026-04-25) (완료)
- [x] **추가 지침 textarea 적용 경로 검증**
    - [x] 과세특과 동아리 세특의 추가 지침 textarea가 `additionalInstructions` 상태로 연결되는지 확인
    - [x] 로컬 LLM 생성 시 추가 지침이 시스템 메시지와 사용자 프롬프트 앞뒤에 반복 삽입되는지 테스트로 고정
    - [x] 내부 검증 재생성 단계에서도 동일한 추가 지침이 계속 전달되는지 확인
- [x] **OpenAI 추가 지침 적용 강도 보강**
    - [x] OpenAI route에서도 추가 지침을 시스템 메시지뿐 아니라 사용자 프롬프트 앞뒤에 반복 삽입하도록 수정
    - [x] `tests/additionalInstructions.test.mjs`를 추가해 로컬 LLM, OpenAI helper, OpenAI route의 추가 지침 전달을 검증

## Phase 38: 출력 토큰 여유 확대 (2026-04-25) (완료)
- [x] **모든 글자수 목표의 출력 토큰 한도 확대**
    - [x] `getMaxTokensForTargetChars()` 계산식을 `targetChars * 2.2`에서 `targetChars * 3.4`로 변경
    - [x] 최소 토큰 한도를 `256`에서 `512`로, 최대 토큰 한도를 `1024`에서 `2048`로 확대
    - [x] 200자 목표는 `680`, 330자 목표는 `1122`, 490자 목표는 `1666` tokens로 증가
    - [x] 직접 입력 글자수도 `normalizeTargetChars()`로 정규화된 뒤 같은 토큰 계산식을 적용
- [x] **재시도 프롬프트 보강**
    - [x] 문장 끊김 재시도 시 `N자 이하`, `핵심 내용만`, `줄바꿈 없이 본문만` 조건을 명시해 반복 재시도 가능성을 줄임

## Phase 39: 로컬 LLM 재시도 원인 분석 및 검증 보정 (2026-04-25) (완료)
- [x] **실제 로컬 모델 호출 검증**
    - [x] `gemma4:e4b` 490자 목표를 직접 호출해 `max_tokens=1666`, 1회 시도, 내부 검증 통과를 확인
    - [x] 기존 `max_tokens=727` 로그는 이전 계산식 기준이므로 최신 코드에서는 330자 목표 `1122`, 490자 목표 `1666`으로 동작함을 확인
- [x] **불필요한 재시도 원인 수정**
    - [x] `지님.`, `나타남.` 같은 세특에서 자연스러운 명사형 종결어미를 완결 문장으로 인정하도록 보정
    - [x] `분석 결과를 바탕으로` 같은 실제 활동 서술을 메타 문구로 오판하지 않도록 검증 정규식 범위를 축소
- [x] **회귀 테스트 보강**
    - [x] 완결 종결어미 오판 방지 테스트 추가
    - [x] 활동 서술 내 `분석 결과` 표현이 검증을 통과하는지 테스트 추가

## Phase 40: 목표 글자수 하한 검증 추가 (2026-04-25) (완료)
- [x] **과소 생성 원인 수정**
    - [x] 기존 내부 검증이 글자수 상한만 확인해 330자 목표에서 163자, 490자 목표에서 181자도 통과하던 구조 확인
    - [x] 목표 글자수의 80%를 내부 하한으로 적용해 지나치게 짧은 결과는 조용히 재생성하도록 수정
    - [x] `높여감.` 같은 세특형 종결도 완전한 문장으로 인정하도록 종결 패턴 보강
- [x] **로컬 모델 재검증**
    - [x] `gemma4:e4b` 330자 목표 직접 호출 결과 313자, 검증 통과 확인
    - [x] `gemma4:e4b` 490자 목표 직접 호출 결과 480자, 검증 통과 확인
- [x] **테스트 보강**
    - [x] 목표 글자수 미달 감지 테스트 추가
    - [x] 최소 목표 글자수 계산 테스트 추가

## Phase 41: 생성 결과 byte 표시 추가 (2026-04-25) (완료)
- [x] **결과 카드 UI 보강**
    - [x] 과세특, 동아리 세특, 행발, 가정통신문의 학생별 생성 결과 아래에 byte 표시 추가
    - [x] 복사 버튼 왼쪽에 `1,234 byte` 형식으로 표시되도록 배치
    - [x] 결과 textarea를 직접 수정해도 표시 byte가 즉시 다시 계산되도록 구성
- [x] **공통 byte 계산 함수 추가**
    - [x] UTF-8 기준 byte 계산 함수 `getUtf8ByteLength()` 추가
    - [x] 한글 1글자 3byte 기준 테스트 추가

## Phase 42: 목표 글자수 90% 이상 생성 기준 적용 (2026-04-25) (완료)
- [x] **내부 검증 하한 상향**
    - [x] 기존 80% 하한을 선택한 글자수 제한의 90% 이상으로 변경
    - [x] 직접 입력 글자수에도 동일하게 90% 이상 기준이 적용되도록 공통 계산 함수 수정
- [x] **프롬프트 목표 구간 동기화**
    - [x] 100자, 200자, 250자 등 짧은 제한도 모델 요청 목표가 90% 이상으로 표시되도록 수정
    - [x] 330자와 490자는 기존처럼 더 길게 유도하되 내부 검증 하한은 90%로 통일
- [x] **테스트 보강**
    - [x] `getMinimumTargetChars()`가 제한의 90%를 반환하는지 테스트
    - [x] `getPromptCharLimit()`가 모든 제한에서 최소 90% 이상을 요청하는지 테스트

## Phase 43: NVIDIA NIM Cloud 모델 추가 (2026-04-25) (완료)
- [x] **로컬 AI 모델 목록 확장**
    - [x] `로컬 AI 모델` 드롭다운에 `(Cloud) Nemotron 3 Super 120B`, `(Cloud) Kimi K2.5`, `(Cloud) GLM 5.1`, `(Cloud) Gemma 4 31B` 추가
    - [x] 긴 NVIDIA 모델 ID는 짧은 표시명으로 축약하고 속도·품질 비교 설명 추가
- [x] **NVIDIA 서버 호출 경로 추가**
    - [x] `/api/nvidia-generate` route 추가
    - [x] `.env`의 `NVIDIA_API_KEY`, `NVIDIA_API_URL`, `NVIDIA_MODEL`을 서버에서만 사용하도록 구성
    - [x] 선택 모델이 `nvidia:`로 시작하면 OpenAI key 적용 여부와 무관하게 NVIDIA NIM route로 생성하도록 네 화면 연결
- [x] **실제 API 검증**
    - [x] NVIDIA `/v1/models` 조회로 요청 모델 4종 사용 가능 여부 확인
    - [x] 새 앱 route 경유로 4개 모델 모두 HTTP 200 및 `OK` 응답 확인
    - [x] NVIDIA helper/route/model-list 테스트 추가

## Phase 44: byte 기준 90% 하한 검증 강화 (2026-04-25) (완료)
- [x] **1500byte 과소 생성 방지**
    - [x] `1500byte` 선택 시 `490자`, `1350byte 이상`, `max_tokens=1666` 경로로 계산되는지 확인
    - [x] `1000byte` 선택 시에만 `330자`, `900byte 이상`, `max_tokens=1122` 경로가 되도록 확인
    - [x] 980byte처럼 `1500byte`의 90%에 못 미치는 결과는 내부 검증 실패로 처리하도록 수정
- [x] **검증 실패 결과 표시 차단**
    - [x] 최대 재시도 후에도 byte/글자수 하한을 못 맞춘 결과를 성공 결과로 표시하지 않도록 변경
    - [x] 최종 검증 실패 시 `status=error`와 알림으로 처리해 사용자가 짧은 결과를 성공으로 오해하지 않도록 수정
- [x] **테스트 보강**
    - [x] preset별 byte 제한 정규화 테스트 추가
    - [x] byte 기준 90% 미달 검증 테스트 추가
    - [x] 검증 실패 결과가 최종 성공으로 반환되지 않는지 테스트 추가

## Phase 45: Cloud API 안정화 및 byte 기반 분량 재설계 (2026-04-25) (완료)
- [x] **byte 제한 충돌 수정**
    - [x] `1500byte=490자` 고정값 때문에 490자를 채워도 1350byte에 못 미치는 구조 확인
    - [x] byte 제한을 1차 기준으로 두고, 글자수는 모델 분량 유도용 보조값으로 재계산하도록 수정
    - [x] `maxTargetBytes` 검증을 추가해 90% 이상뿐 아니라 선택 byte 초과도 내부 검증에서 차단
- [x] **검증 보정 프롬프트 강화**
    - [x] 내부 검증 보정 시 원래 작성 조건을 함께 제공해 짧은 결과를 같은 의미만 유지하며 다시 짧게 쓰는 문제 수정
    - [x] 보정 재시도를 4회까지 늘리고, 부족하면 활동 과정·근거·태도·변화를 촘촘히 확장하도록 지시
- [x] **NVIDIA/OpenAI API 안정화**
    - [x] `.env`의 `NVIDIA_MODEL`을 허용된 NVIDIA 모델 ID로 수정
    - [x] NVIDIA route에서 502/503/504/429와 HTML 오류 응답을 재시도하도록 보강
    - [x] OpenAI route에서 표시 가능한 본문이 비어 있을 때 1회 재요청하고 Responses 형식의 `output_text`도 읽도록 보강
- [x] **진행 상태 표시 개선**
    - [x] 학생별 생성 중 overlay에 `웹 검색 보강 중`, `NVIDIA Cloud 모델 접속 및 생성 중`, `내부 검증 보정 재시도` 등 단계 표시 추가
    - [x] 과세특 웹 검색 보강 체크박스를 기본 해제 상태로 변경
    - [x] 개별 활동 내용이 없으면 체크되어 있어도 웹 검색 보강 없이 생성하는 조건 유지 확인

## Phase 46: 생성 진행 상태 문구 세분화 (2026-04-25) (완료)
- [x] **접속/생성 단계 분리**
    - [x] NVIDIA, OpenAI, 로컬 LLM 모두 `접속 중 → 접속 완료, 생성 요청 중 → 생성 중` 순서로 표시
    - [x] 네 화면의 반복 progress 처리 코드를 `utils/generationProgress.js` 공통 유틸로 통합
- [x] **사용자 친화 문구 적용**
    - [x] `내부 검증 보정 재시도` 같은 개발자 표현을 제거
    - [x] 분량 부족, 분량 초과, 문장 마무리, 표현 규칙 수정 사유에 따라 쉬운 문구로 표시
- [x] **검증**
    - [x] 진행 문구 유틸 테스트 추가
    - [x] 기존 전체 테스트와 프로덕션 빌드 통과 확인

## Phase 47: Why-How-What-Learn 생성 유도 강화 (2026-04-25) (완료)
- [x] **공통 생성 지침 보강**
    - [x] 공통 분량 지침에 `Why(동기)-How(과정)-What(결과)-Learn(성장)` 확장 방식을 추가
    - [x] 입력된 활동의 과정, 근거, 태도, 변화, 수행 장면을 구체화하되 새 활동·새 사실은 지어내지 않도록 명시
    - [x] 과세특, 동아리 세특, 행발, 가정통신문이 같은 공통 지침을 사용하도록 기존 연결 구조 유지
- [x] **내부 보정 프롬프트 보강**
    - [x] 분량 부족 보정 시 동기, 과정, 결과, 성장 단서를 확장하도록 수정
    - [x] 원래 작성 조건과 입력 활동 단서를 유지하고 허위 사실을 만들지 않도록 재지시
- [x] **테스트 보강**
    - [x] 공통 분량 지침에 Why-How-What-Learn 구조가 포함되는지 테스트 추가
    - [x] 내부 보정 프롬프트가 입력 단서 유지와 확장 규칙을 포함하는지 테스트 추가
- [x] **검증**
    - [x] `npm test` 전체 40개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인

## Phase 48: 생성 안정화, LM Studio 연동, 모델 목록 재정리 (2026-04-25) (완료)
- [x] **내부 검증 정책 현실화**
    - [x] 목표 분량 하한을 선택 byte의 `85%` 기준으로 완화
    - [x] `분량 부족`, `문장 종결 불완전`만으로는 실패 처리하지 않고 결과를 반환하도록 변경
    - [x] 보정 재시도 기본값을 `4회`에서 `1회`로 축소해 `분량과 문장을 다듬는 중...` 단계에서 오래 머무는 문제 완화
    - [x] 최종 결과가 실제로 검증을 통과했는데도 `규칙 미충족` 오류가 나던 경로 수정
- [x] **LM Studio 모델 자동 사용 경로 추가**
    - [x] `.env`에 `LMSTUDIO_API_URL`, `LMSTUDIO_API_KEY`, `LMSTUDIO_GEMMA_26B_MODEL` 추가
    - [x] `Gemma 4 26B Q4 - 속도 느림, 품질 높음` 모델을 로컬 모델 4번째로 추가
    - [x] 해당 모델 선택 시 사용자가 URL/API key를 입력하지 않아도 `https://lm.alluser.site`와 모델 identifier `gemma-4-26b-a4b-it`를 자동 사용하도록 구성
    - [x] 실제 파일 기준 `Q4_K_S` 양자화 GGUF 모델이므로 UI 표시명을 `Gemma 4 26B Q4`로 정리
- [x] **LM Studio/NPM 연결 원인 분석**
    - [x] `https://lm.alluser.site/v1/models`의 `502 Bad Gateway`가 앱 코드 문제가 아니라 NPM upstream 연결 문제임을 확인
    - [x] Mac mini에서 `127.0.0.1:1234/v1/models`는 성공하고 `192.168.0.207:1234`는 실패하는 상태를 확인
    - [x] LM Studio Local Server의 LAN 접근 허용 후 `192.168.0.207:1234` 연결 가능 상태로 전환
- [x] **OpenAI 생성 경로 보정**
    - [x] GPT-5 계열에서 `finish_reason=length`와 빈 응답이 발생하는 원인을 `max_completion_tokens` 부족으로 분석
    - [x] OpenAI route에 `reasoning_effort: "minimal"` 적용
    - [x] OpenAI `max_completion_tokens`를 최소 `4096`으로 확대해 reasoning token이 본문 예산을 모두 소모하는 문제 완화
- [x] **NVIDIA 모델 목록 정리**
    - [x] 사용자 선택 목록에서 NVIDIA Cloud 모델 제거
    - [x] 기본 모델을 다시 `gemma4:e4b`로 복구
    - [x] NVIDIA route/helper 테스트는 유지하되, UI 모델 목록에는 표시되지 않도록 테스트 기대값 변경
- [x] **로컬 모델별 max token 보정**
    - [x] `gemma4:e2b`: 기존 계산값 유지, 1500byte 기준 `2003 tokens`
    - [x] `gemma4:e4b`: 기본 모델 안정성을 위해 최소 `3072 tokens` 적용
    - [x] `gemma4:26b`: 빈 응답 및 `finish_reason=length` 완화를 위해 최소 `4096 tokens` 적용
    - [x] `Gemma 4 26B Q4` LM Studio 모델도 최소 `4096 tokens` 적용
- [x] **마무리 접속 표현 금지 강화**
    - [x] 과세특, 동아리세특, 행발, 가정통신문 프롬프트에 `마지막으로`, `끝으로`, `마무리하며`, `덧붙여`, `추가로` 사용 금지 추가
    - [x] `utils/generationHarness.js`에 공통 마무리 접속어 검증 패턴 추가
    - [x] 내부 보정 프롬프트와 sanitize 단계에서도 해당 표현을 제거/회피하도록 보강
- [x] **테스트 보강 및 검증**
    - [x] LM Studio 모델 순서, endpoint, API key, 모델 identifier 전달 테스트 추가
    - [x] OpenAI reasoning/min token 요청 body 테스트 추가
    - [x] 모델별 max token 보정 테스트 추가
    - [x] record/letter 모두에서 `마지막으로`가 검증 위반으로 잡히는 테스트 추가
    - [x] 최종 `npm test` 전체 47개 테스트 통과 확인

## Phase 49: LM Studio 기반 로컬 모델 3종 통합 (2026-04-26) (완료)
- [x] **로컬 모델 목록 재정리**
    - [x] 사용자 선택 목록을 `Gemma 4 E4B`, `Gemma 4 E2B`, `Gemma 4 26B Q4` 3개로 정리
    - [x] 기존 원격 Ollama용 `gemma4:26b` 항목 제거
    - [x] 세 모델 모두 `https://lm.alluser.site` LM Studio 엔드포인트를 사용하도록 `utils/streamFetch.js` 구성 변경
- [x] **기본 모델 변경**
    - [x] 기본 모델을 `Gemma 4 26B Q4`로 변경
    - [x] 드롭다운 표시 문구를 26B Q4 기준으로 정리
    - [x] 표시 문구를 `Gemma 4 E4B - 기본보다 빠름, 품질 보통`, `Gemma 4 E2B - 가장 빠름, 품질 간단`, `Gemma 4 26B Q4 - 기본 모델, 속도 느림, 품질 높음`으로 변경
- [x] **LM Studio 실제 모델 identifier 반영**
    - [x] LM Studio `/v1/models` 조회 결과를 확인해 실제 모델 ID 반영
    - [x] e4b 내부 요청 모델을 `google/gemma-4-e4b`로 설정
    - [x] e2b 내부 요청 모델을 `google/gemma-4-e2b`로 설정
    - [x] 26B Q4 내부 요청 모델은 `gemma-4-26b-a4b-it` 유지
    - [x] `.env`에 `LMSTUDIO_GEMMA_E4B_MODEL`, `LMSTUDIO_GEMMA_E2B_MODEL`, `LMSTUDIO_GEMMA_26B_MODEL` 정리
- [x] **공통 호출 경로 동기화**
    - [x] `getLocalModelConfig()`를 export해 모델별 endpoint/API key/model identifier를 공통으로 조회 가능하게 변경
    - [x] 디버깅용 `/api/generate` route도 기본 모델의 LM Studio 설정을 따라가도록 변경
    - [x] 로컬 LLM 호출 body에 `reasoning_effort: "none"` 유지
- [x] **검증**
    - [x] 세 로컬 모델 모두 LM Studio endpoint, API key, 실제 모델 identifier로 요청되는지 테스트 추가
    - [x] 기본 모델이 `Gemma 4 26B Q4`인지 테스트 갱신
    - [x] `npm test` 전체 47개 테스트 통과 확인

## Phase 50: 과세특 개선 사항을 동아리 세특·행발에 동기화 (2026-04-26) (완료)
- [x] **superpowers 절차 적용**
    - [x] `superpowers:systematic-debugging` 흐름에 맞춰 과세특 기준 구현을 먼저 분석
    - [x] `superpowers:test-driven-development` 흐름에 맞춰 누락 동작을 테스트로 먼저 고정
    - [x] `tests/pageParity.test.mjs`를 추가하고, 구현 전 동아리/행발 parity 테스트 실패를 확인
- [x] **동아리 세특 보강**
    - [x] 과세특의 `fetchSearchContext` 기반 웹 검색 보강 흐름을 동아리 세특에 적용
    - [x] 학생별 개별 활동 내용이 있을 때 `학생 개별 활동 내용 웹 검색 보강` 옵션으로 활동 주제·개념·쟁점 맥락을 보강하도록 구성
    - [x] 검색 보강 자료는 실제 입력된 개별 활동과 공통 활동을 우선하고, 배경 이해에만 사용하도록 프롬프트에 명시
    - [x] 생성 중 진행 문구에 `웹 검색 보강 중...` 단계 추가
- [x] **행발 작성 보강**
    - [x] 과세특/동아리 세특에 있던 `추가 지침 사항` 입력 UI를 행발 작성에도 추가
    - [x] 행발의 로컬 LLM, OpenAI, NVIDIA 생성 경로 모두에 `additionalInstructions`를 전달하도록 수정
    - [x] 행동 관찰 내용 기반 웹 검색 보강 옵션을 추가해 인성 요소·공동체 역량·지도 관점의 맥락을 보강하도록 구성
    - [x] 검색 보강 자료는 입력된 행동 관찰 내용을 우선하고, 실제 관찰되지 않은 사실을 만들지 않도록 프롬프트에 명시
    - [x] 생성 중 진행 문구에 `웹 검색 보강 중...` 단계 추가
- [x] **검증**
    - [x] 동아리 세특과 행발이 과세특의 검색 보강/추가 지침 흐름을 유지하는지 `pageParity` 테스트 추가
    - [x] `node --experimental-default-type=module --test tests/pageParity.test.mjs` 통과 확인
    - [x] `npm test` 전체 49개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인

## Phase 51: 고등학교용 동아리 세특·행발 내용 품질 기준 반영 (2026-04-26) (완료)
- [x] **적용 범위 정리**
    - [x] 사용자가 제공한 좋은 동아리 특기사항/행동특성 및 종합의견 기준을 프롬프트 품질 기준으로 반영
    - [x] 초등학교와 중학교는 기존 작성 흐름을 유지
    - [x] 고등학교 선택 시에만 심화 내용 기준이 추가되도록 조건부 적용
- [x] **동아리 세특 고등학교 심화 기준 추가**
    - [x] `utils/recordQualityGuidance.js`에 `getClubHighSchoolQualityGuidance()` 추가
    - [x] 고등학교 동아리 세특에 문제의식, 교과·전공 연결, 구체적 역할, 시행착오와 개선, 팀 활동 속 기여, 후속 탐구와 성장 기준 반영
    - [x] 핵심 흐름을 `문제의식 → 교과·전공 개념 연결 → 구체적 탐구 과정 → 본인의 역할 → 난관과 수정 → 결과 또는 기여 → 후속 탐구와 성장`으로 안내
    - [x] 초등/중학교 선택 시 해당 심화 기준이 빈 문자열로 반환되도록 처리
- [x] **행발 고등학교 심화 기준 추가**
    - [x] `utils/recordQualityGuidance.js`에 `getBehaviorHighSchoolQualityGuidance()` 추가
    - [x] 행발 작성 화면에 학교급 선택값 추가
    - [x] 고등학교 행발에 학교생활 전반, 지속적으로 관찰되는 태도, 성실성과 책임감의 행동 근거, 배려와 관계 형성 방식, 공동체에 미친 긍정적 영향, 성장과 변화, 학생만의 이미지 기준 반영
    - [x] 핵심 흐름을 `지속적으로 관찰된 태도 → 구체적 학교생활 장면 → 관계 속 역할 → 공동체에 미친 영향 → 성장 가능성`으로 안내
    - [x] 초등/중학교 선택 시 해당 심화 기준이 빈 문자열로 반환되도록 처리
- [x] **테스트 보강 및 검증**
    - [x] `tests/recordQualityGuidance.test.mjs` 추가
    - [x] 동아리/행발 심화 기준이 고등학교에서만 반환되고 초등/중학교에서는 반환되지 않는지 테스트
    - [x] `tests/pageParity.test.mjs`에 동아리/행발 페이지가 고등학교 조건부 지침 함수를 연결하는지 테스트 추가
    - [x] `npm test` 전체 53개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인

## Phase 52: 과세특 활동별 A/B/C 성취도 적용 (2026-04-26) (완료)
- [x] **적용 범위 정리**
    - [x] 과세특의 기존 학생별 단일 A/B/C 등급 구조를 점검
    - [x] 초등학교, 중학교, 고등학교 모두 활동별 A/B/C 성취도 선택을 사용하도록 범위 확정
    - [x] 고등학교 선택 시에도 A/B/C 선택 UI와 프롬프트 반영이 동일하게 적용되도록 처리
- [x] **활동별 A/B/C 데이터 구조 변경**
    - [x] 학생 데이터에 `activityGrades` 배열을 추가해 활동 순서와 성취도를 1:1로 연결
    - [x] 활동을 추가하면 기존 학생에게도 새 A/B/C 칸이 자동 생성되도록 동기화
    - [x] 활동을 삭제하면 해당 순서의 A/B/C 값도 함께 제거되도록 처리
    - [x] 엑셀 업로드 또는 학생 수 변경으로 새 학생이 생겨도 현재 활동 수만큼 A/B/C 값이 생성되도록 보강
- [x] **UI 보완**
    - [x] 학생 카드 안에 `활동별 성취도` 영역을 추가
    - [x] 활동 1개당 한 줄씩 A/B/C 버튼 3개를 표시하도록 변경
    - [x] 버튼을 `btn-grade-sm` 크기로 줄이고, 학생 1인당 카드 높이가 과도하게 커지지 않도록 CSS 추가
    - [x] 활동별 성취도 패널을 한 줄에 2개 활동씩 표시하도록 압축하고, 좁은 화면에서는 1열로 내려가도록 보완
    - [x] 활동명과 A/B/C 버튼은 붙여 보이게 하고, 활동 1·2·3 항목 사이 간격은 넓혀 구분감 보강
- [x] **생성 프롬프트 보완**
    - [x] 활동 텍스트와 선택된 A/B/C 값을 함께 묶은 `validActivityEntries`/`selectedActivityEntries` 구조로 생성 흐름 변경
    - [x] 개별 활동 관련성 정렬 또는 랜덤 셔플 후에도 각 활동의 원래 A/B/C 값이 따라가도록 처리
    - [x] 프롬프트에 활동별 A/B/C 반영 기준을 추가해 활동마다 깊이와 구체성을 다르게 반영
    - [x] 고등학교 프롬프트에서도 A/B/C 기준 문구가 동일하게 들어가도록 처리
    - [x] 엑셀 다운로드 시 모든 학교급에서 `활동별 성취도`를 함께 내보내도록 변경
- [x] **테스트 보강 및 검증**
    - [x] `tests/gwasetukActivityGrades.test.mjs` 추가
    - [x] 구현 전 테스트 실패를 확인한 뒤 TDD 방식으로 구현
    - [x] 활동별 A/B/C 상태 동기화, 모든 학교급 표시, 활동별 등급 프롬프트 반영 테스트 추가
    - [x] 활동별 성취도 패널이 2열 grid로 배치되는지 CSS 테스트 추가
    - [x] 활동명과 A/B/C 버튼 내부 간격, 활동 항목 사이 간격을 CSS 테스트로 고정
    - [x] `npm test -- tests/gwasetukActivityGrades.test.mjs` 통과 확인

## Phase 53: 추가 지침 기반 활동 4개 랜덤 선별 (2026-04-26) (완료)
- [x] **원인 확인**
    - [x] `n_keep >= n_ctx` 오류는 모델 출력이 아니라 입력 프롬프트가 4096 토큰 컨텍스트를 넘어서 발생함을 확인
    - [x] LM Studio OpenAI 호환 `/v1/chat/completions` 요청에서는 컨텍스트 길이를 요청마다 지정할 수 없고, native `/api/v1/chat`/모델 관리 API는 현재 서버에서 403으로 막혀 있음을 확인
    - [x] 긴 프롬프트 실서버 호출로 `n_ctx: 4096` 응답을 확인해 8192 컨텍스트가 아직 서버에 적용되지 않았음을 확인
- [x] **활동 선별 보완**
    - [x] `활동 내용 중 4개를 랜덤으로 선택한다` 계열 추가 지침을 감지하는 `utils/activitySelection.js` 추가
    - [x] 과세특에서 해당 추가 지침이 있으면 모델 호출 전에 셔플된 활동 목록을 최대 4개로 실제 축소
    - [x] 동아리 세특에서 해당 추가 지침이 있으면 관련성 정렬/시간순 유지보다 랜덤 4개 선택을 우선 적용
    - [x] 350자 이하 분량별 기존 활동 개수 제한은 공통 유틸로 유지
    - [x] 개별 활동 내용에 책 제목이 있어도 첫 문장은 선택된 활동1 공통 활동으로 시작하도록 과세특/동아리 프롬프트 강화
- [x] **테스트 보강**
    - [x] `tests/activitySelection.test.mjs` 추가
    - [x] 추가 지침 감지, 목표 글자수별 활동 제한, 과세특/동아리 연결 여부, 활동1 우선 시작 규칙을 테스트로 고정
    - [x] `npm test` 전체 74개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인

## Phase 54: 동아리 세특 첫 문장 표현 다양화 (2026-04-26) (완료)
- [x] **원인 확인**
    - [x] 동아리 세특 프롬프트가 활동1 시작을 강하게 요구하면서 `과학 실험 보고서 작성에서`처럼 활동명 그대로 시작하는 반복이 생길 수 있음을 확인
    - [x] 개별 활동 내용의 책 제목/주제가 첫 활동처럼 앞서지 않게 한 기존 규칙은 유지
- [x] **프롬프트 보완**
    - [x] 활동1의 핵심 소재는 유지하되 활동명을 그대로 베껴 시작하지 않도록 지시 추가
    - [x] `~보고서 작성을 통해`, `~를 탐구하는 과정에서`, `~에 대한 고민을 가지고` 등 시작 표현 변주 예시 추가
    - [x] `과학 실험 보고서 작성에서`처럼 고정된 명사구 시작을 피하도록 명시
- [x] **테스트 보강**
    - [x] `tests/activitySelection.test.mjs`에 동아리 시작 표현 다양화 회귀 테스트 추가
    - [x] 구현 전 실패를 확인한 뒤 프롬프트 보완으로 통과 확인

## Phase 55: 동아리 세특 시작 방식 랜덤 다양화 (2026-04-26) (완료)
- [x] **원인 확인**
    - [x] 예시 문구를 직접 제시하면 모델이 해당 문구를 그대로 따라 시작할 수 있음을 확인
    - [x] 활동1 공통 활동으로 시작하는 구조는 유지하되, 시작 방식 자체를 다양화하는 방향으로 정리
- [x] **프롬프트 보완**
    - [x] 동아리 세특 생성마다 `openingStyleGuides` 중 하나를 랜덤 선택해 `첫 문장 시작 방식`으로 전달
    - [x] 문제의식형, 탐구 과정형, 보고서 정리형, 실험 설계형, 자료 분석형, 협업 토의형, 한계 개선형, 발표 공유형 시작 방식을 추가
    - [x] 선택된 시작 방식과 예시 표현을 그대로 복사하지 말고 입력 활동에 맞춰 새 문장으로 변주하도록 명시
- [x] **테스트 보강**
    - [x] `tests/activitySelection.test.mjs`에 랜덤 시작 방식 후보와 선택 로직 확인 추가
    - [x] 구현 전 실패를 확인한 뒤 통과 확인

## Phase 56: 과세특 A/B/C 기준 재정렬 및 고등학교 동일 적용 (2026-04-27) (완료)
- [x] **C등급 생성 기준 재정의**
    - [x] 기존 `C(보통)` 기준을 `C(노력 필요)` 기준으로 조정
    - [x] A는 최고 수준의 성취, B는 노력하며 수행한 모습, C는 기준점에 충분히 도달하지 못해 추가 노력이 필요한 모습으로 서술 기조 구분
    - [x] C등급 활동에는 기초 이해의 제한, 보완해야 할 학습 요소, 추가적인 노력이 필요한 지점을 분명히 드러내도록 프롬프트 보완
    - [x] `못함`, `소극적`, `미흡함` 같은 비판적 낙인 표현은 사용하지 않도록 안전 기준 유지
- [x] **고등학교 A/B/C 적용 범위 확대**
    - [x] 고등학교 선택 시에도 학생 카드의 `활동별 성취도` A/B/C 버튼이 항상 표시되도록 수정
    - [x] 고등학교 과세특 생성 프롬프트에도 활동별 A/B/C 기준이 초등학교·중학교와 동일하게 반영되도록 수정
    - [x] 엑셀 다운로드 시 모든 학교급에서 `활동별 성취도` 열이 함께 저장되도록 변경
- [x] **테스트 및 문서 동기화**
    - [x] `tests/gwasetukActivityGrades.test.mjs`에 C등급 새 기준과 모든 학교급 A/B/C 적용 회귀 테스트 반영
    - [x] `task.md`의 기존 고등학교 제외 설명을 현재 동작 기준으로 정리
    - [x] `npm test` 전체 75개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인
    - [x] `git diff --check` 통과 확인
- [x] **GitHub 업로드**
    - [x] 커밋 `48c194a feat: align gwasetuk activity grade rules` 생성
    - [x] `origin/master`로 푸시 완료
    - [x] 원격 HEAD와 로컬 HEAD가 `48c194a905e2db10ea0bee6d5cb5d6bbf5bc0889`로 일치함을 확인

## Phase 57: 가정통신문 조언형 생성 규칙 및 로컬 LLM 검증 강화 (2026-05-19) (완료)
- [x] **키워드 해석 방식 재정의**
    - [x] 가정통신문의 공통 키워드를 학생 관찰 사실이 아니라 `방학 조언 영역`으로 사용하도록 변경
    - [x] `학업, 건강, 친구관계, 가족관계`를 목록처럼 나열하지 않고 학교생활을 성실하게 수행했다는 서사 뒤에 자연스럽게 연결하도록 프롬프트 수정
    - [x] 입력되지 않은 구체 활동, 실험, 탐구 주제, 수행 장면을 지어내지 않도록 금지 규칙 추가
    - [x] 학교생활 평가는 과거 경어체(`기여했습니다`, `인상적이었습니다`, `돋보였습니다`)로 쓰고, 방학 조언은 권유형 경어체로 마무리하도록 정리
- [x] **다양성 및 서사 흐름 보강**
    - [x] `utils/letterKeywords.js`에 생성 방향 프로필을 추가해 시작 관점과 가정 연계 관점이 매번 달라지도록 구성
    - [x] 키워드 순서를 셔플하되, 결과에는 쉼표 나열이 아니라 문장 흐름으로 녹여 쓰도록 지시
    - [x] 문장들이 따로 놀지 않도록 성실한 학교생활 평가에서 방학 생활 조언으로 이어지는 흐름을 강화
- [x] **내부 검증 및 보정 강화**
    - [x] `utils/generationHarness.js`에 가정통신문 조언 영역 검증을 추가해 학업, 건강, 친구관계, 가족관계가 모두 반영되는지 확인
    - [x] 조언 영역이 빠진 경우 계절 필수어(`여름방학`, `겨울방학`, `새 학기`)를 보존하면서 보정 문장을 추가하거나 마지막 안내 문장을 교체하도록 처리
    - [x] 가정통신문도 다른 생성 결과와 같이 byte 기준 글자수 검증을 적용하되, 분량 부족만 있는 경우는 경고로 허용하는 정책 유지
- [x] **로컬 LLM 및 API 경로 확인**
    - [x] 로컬 LLM 기본 호출 경로가 `https://lm.alluser.site/v1/chat/completions`를 사용하고 `Origin`/`Referer` 헤더도 같은 도메인으로 전달되는지 테스트로 확인
    - [x] OpenAI, NVIDIA, 로컬 LLM 모두 가정통신문 전용 시스템 메시지와 `outputType: "letter"`를 유지하도록 회귀 테스트 추가
    - [x] 실제 `lm.alluser.site` API로 여름방학 2개, 겨울방학 2개 샘플을 생성해 계절어와 조언 영역 검증을 확인
- [x] **검증**
    - [x] `npm test` 전체 94개 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인
    - [x] `git diff --check` 통과 확인

## Phase 58: OpenAI GPT-5.4 nano 단일화 및 1500byte 생성 검증 (2026-06-12) (완료)
- [x] **OpenAI 모델 비용 최적화**
    - [x] OpenAI 선택 모델을 `gpt-5.4-nano` 하나로 단일화
    - [x] 브라우저 localStorage나 이전 요청에 남은 `gpt-5-mini`, `gpt-5.4-mini` 값은 자동으로 `gpt-5.4-nano`로 보정
    - [x] `gpt-5.4-nano`가 지원하지 않는 `reasoning_effort: "minimal"` 대신 `reasoning_effort: "none"`으로 변경
- [x] **1500byte 생성 보강**
    - [x] 1500byte 설정에서 모델이 짧게 요약하지 않도록 430~500자 가시 문자 범위 안내를 추가
    - [x] byte 미달 보정 프롬프트에 현재 byte와 부족 byte를 명시해 재작성 방향을 분명히 함
    - [x] 입력 단서가 충분한 실제 사용형 과세특 케이스에서 `gpt-5.4-nano`가 1352byte 결과를 생성해 1275~1500byte 기준을 만족함을 확인
- [x] **검증**
    - [x] `.env`의 OpenAI API key로 직접 OpenAI 호출 성공 확인 (`gpt-5.4-nano-2026-03-17`, 응답 `OK`)
    - [x] 앱의 `/api/openai-generate` 경유 호출 성공 확인 (`model: gpt-5.4-nano`)
    - [x] `npm test` 전체 테스트 통과 확인
    - [x] `npm run build` 프로덕션 빌드 통과 확인
